{
  "name": "fabrix-framework",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "type-check": "turbo type-check",
    "changeset:publish": "pnpm build && changeset publish"
  },
  "packageManager": "pnpm@9.9.0",
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.0",
    "@changesets/cli": "^2.27.12",
    "turbo": "^2.3.3"
  }
}
